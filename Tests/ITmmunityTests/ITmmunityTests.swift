import XCTest
@testable import ITmmunity

final class ITmmunityTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ITmmunity().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
